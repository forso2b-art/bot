# 🤖 Telegram Task & User Management Bot

Многофункциональный Telegram-бот на **Python (aiogram)** с системой задач, пользователей, ролей и автоматических санкций.  
Проект полностью готов для **публичного релиза** и безопасного развёртывания через **GitHub** (включая хостинг **bothost.ru**).

---

## 🚀 Возможности

- 📝 Создание и управление задачами
- 👥 Работа с пользователями
- 🔐 Система ролей (администраторы / обычные пользователи)
- ⚠️ Предупреждения пользователям
- ⛔ Автоматический бан при превышении лимита
- 💾 **Персистентное хранение данных**
- 🔄 Переживает перезапуск и обновления из GitHub

---

## 🧰 Технологии

- Python 3.10+
- aiogram
- JSON-хранилище (с возможностью перехода на SQLite)
- GitHub Deploy Friendly Architecture

---

## 📁 Структура проекта

```text
bot/
 ├── main.py            # основной файл бота
 ├── README.md          # документация
 └── data/              # постоянное хранилище данных
     └── .gitkeep       # нужен для сохранения папки в GitHub
```

> ⚠️ Файлы с данными создаются **на сервере** и **не коммитятся** в репозиторий.

---

## 💾 Хранение данных

Бот автоматически создаёт файлы в папке `data/`:

- `bot_data.json` — задачи, пользователи, прогресс
- `bot_security_state.json` — роли, баны, предупреждения

📌 Файлы создаются **только при первом сохранении данных**, а не при запуске.

---

## ⚙️ Конфигурация

Основные параметры находятся в начале файла `main.py`:

```python
BOT_TOKEN = "YOUR_BOT_TOKEN"
CREATOR_ID = 123456789
ADMIN_IDS = {123456789}

DATA_DIR = "data"
DATA_FILE = "data/bot_data.json"
STATE_FILE = "data/bot_security_state.json"

WARN_LIMIT = 3
AUTO_BAN_HOURS = 24
PURGE_TASKS_ON_BAN = True
```

---

## 🧪 Проверка сохранения данных

1. Запустите бота
2. Создайте задачу или выдайте предупреждение
3. Перезапустите бота
4. Убедитесь, что данные сохранились

Если данные остались — всё работает корректно ✅

---

## 🌐 Развёртывание (bothost.ru)

1. Репозиторий должен быть **публичным**
2. В репозитории обязательно должна быть папка:
   ```
   data/.gitkeep
   ```
3. В настройках bothost:
   - Git URL → репозиторий
   - Ветка → `main`
4. Нажмите **«Создать бота»**

❗ Переменные окружения не требуются (на бесплатном тарифе).

---

## 🛑 Важно

- ❌ Не коммитьте файлы `bot_data.json` и `bot_security_state.json`
- ❌ Не удаляйте папку `data/` на сервере
- ❌ Не используйте самоизменяющийся код

---

## 🔮 Возможные улучшения

- 📦 Переход на SQLite
- 🔁 Автобэкапы данных
- 🌐 Внешняя база данных (PostgreSQL / Supabase)
- 🧩 Выделение логики БД в отдельный модуль

---

## 📜 Лицензия

Проект распространяется свободно.  
Вы можете использовать, изменять и развёртывать бота для личных и учебных целей.

---
